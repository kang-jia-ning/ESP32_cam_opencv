### 问题分析
用户提供了一个豆包API的curl调用示例，与我之前实现的豆包API调用有一些差异：
1. 模型名称不同：示例中使用的是 "doubao-seed-1-6-251015"
2. 参数名称不同：示例中使用的是 "max_completion_tokens" 而非 "max_tokens"
3. 消息格式不同：示例支持多模态消息，包含 "image_url" 和 "text" 类型
4. 新增了 "reasoning_effort" 参数

### 修复方案
1. 修改配置文件，添加对新模型名称和参数的支持
2. 修改 `ask_question` 函数，支持多模态消息格式和新的参数
3. 确保修改后的代码能够正确处理纯文本和多模态消息
4. 测试修改后的代码，确保能够正确调用豆包API

### 修复步骤
1. 打开 `config.py` 文件，添加对新模型名称和参数的支持
2. 打开 `app/routes/main.py` 文件，修改 `ask_question` 函数
3. 调整豆包API调用的参数名称和消息格式
4. 确保修改后的代码能够正确处理纯文本和多模态消息
5. 测试修改后的功能

### 预期结果
修复后，系统将能够使用用户提供的豆包API调用示例中的参数和消息格式，正确调用豆包API，支持多模态消息和新的参数。